services:
  - type: web
    name: comment-crawler
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m playwright install chromium && uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.7
